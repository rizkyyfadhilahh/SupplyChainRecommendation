# ============================================================
# Supply Chain Planning API — Environment Configuration
# Rename file ini ke .env dan isi nilainya.
# Jangan commit .env ke version control.
# ============================================================

# ── Security ────────────────────────────────────────────────
# API key untuk autentikasi request.
# Generate dengan: python -c "import secrets; print(secrets.token_hex(32))"
API_KEY=your-secret-api-key-here

# Comma-separated list of allowed CORS origins.
ALLOWED_ORIGINS=http://localhost:3000

# ── Database ────────────────────────────────────────────────
# Set ke "false" untuk CSV-only mode (in-memory, tanpa SQLite).
USE_SQLITE=true

# Optional: pakai PostgreSQL sebagai pengganti SQLite.
# DATABASE_URL=postgresql://user:password@host:5432/supply_chain

# ── Data Files ──────────────────────────────────────────────
# Direktori tempat file CSV disimpan.
# Default: <project_root>/backend/temp_data
# DATA_DIR=/data/supply_chain

# Nama file dalam DATA_DIR.
# Subdirektori diperbolehkan, contoh: "3 months/events_bc.csv"
# Default values ditampilkan — hanya set jika nama file berbeda.
# MASTER_FACILITY_FILENAME=master_facility.csv
# EVENTS_BC_FILENAME=3 months/events_bc_01_Des_24_Feb.csv
# LINKS_BC_FILENAME=3 months/links_bc_01_Des_24_Feb.csv
# TRANS_MB51_FILENAME=trans_mb51.csv
# RESTAN_MB51_FILENAME=restan_mb51.csv
# SLOC_EUDR_CONFIG_FILENAME=sloc_eudr_config.csv

# ── Performance ─────────────────────────────────────────────
# Jumlah Uvicorn worker processes.
# Keep di 1 saat pakai SQLite (single writer limitation).
# Naikkan ke (2 * CPU_COUNT + 1) saat pakai PostgreSQL + Redis.
WEB_CONCURRENCY=1

NUMEXPR_MAX_THREADS=4
OMP_NUM_THREADS=4

# ── Job Queue ───────────────────────────────────────────────
# Redis URL untuk distributed job store.
# Biarkan kosong untuk pakai in-memory job store (single-process only).
# Wajib diset jika WEB_CONCURRENCY > 1.
# REDIS_URL=redis://localhost:6379/0

# ── Frontend (Next.js) ──────────────────────────────────────
BACKEND_URL=http://localhost:8000
BACKEND_API_KEY=your-secret-api-key-here