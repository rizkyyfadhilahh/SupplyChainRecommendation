# ============================================================
# requirements.docker.txt — Minimal deps untuk Docker image
# Menghindari semua C-extension yang butuh GCC saat compile
# ============================================================

# --- Web Framework ---
fastapi==0.115.0
uvicorn==0.30.6
starlette==0.38.6
python-multipart==0.0.9

# --- Data Layer ---
pandas==2.2.2
numpy==1.26.4
python-dateutil==2.9.0.post0
pytz==2025.2
tzdata==2025.3

# --- Pydantic & Validation ---
pydantic==2.9.2
pydantic_core==2.23.4
annotated-types==0.7.0

# --- Auth ---
python-jose==3.3.0
passlib==1.7.4
bcrypt==5.0.0
rsa==4.9.1
ecdsa==0.19.1
pyasn1==0.6.2

# --- Rate Limiting ---
slowapi==0.1.10

# --- Env & Config ---
python-dotenv==1.0.1

# --- JSON (required for ORJSONResponse in FastAPI) ---
orjson

# --- HTTP ---
requests==2.32.3
httpx==0.27.0
certifi==2026.2.25
charset-normalizer==3.4.4
idna==3.11
urllib3==2.6.3

# --- Database (pure-Python drivers only, no C compilation) ---
PyMySQL==1.1.1          # pure Python MySQL, pengganti mysqlclient
psycopg2-binary==2.9.9  # pre-compiled binary, tidak butuh GCC
SQLAlchemy==2.0.35

# --- Async ---
anyio==4.12.1
sniffio==1.3.1
h11==0.16.0

# --- Utilities ---
packaging==26.0
six==1.17.0
cachetools          # used by gap_analysis_service & drilldown_service (TTLCache)
